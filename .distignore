# Source files (not shipped in the plugin zip)
/node_modules
/src
/package.json
/package-lock.json
.gitignore
.distignore


# WP.org generated

/.phpcs-tools

# Docs (no van en el zip de producción)
/docs
